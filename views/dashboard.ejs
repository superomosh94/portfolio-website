<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        :root {
            --bg-dark: #0f172a;
            --bg-card: #1e293b;
            --text-primary: #f8fafc;
            --text-secondary: #94a3b8;
            --accent-primary: #3b82f6;
            --sidebar-width: 280px;
        }

        body {
            font-family: 'Inter', system-ui, -apple-system, sans-serif;
            background-color: var(--bg-dark);
            color: var(--text-primary);
            margin: 0;
            min-height: 100vh;
        }

        .dashboard-layout {
            max-width: 1400px;
            margin: 0 auto;
        }

        /* Nav is handled by partial, layout adjusted */

        .dashboard-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 2rem;
            padding: 2rem;
        }

        .dashboard-card {
            background: var(--bg-card);
            padding: 2.5rem 2rem;
            border-radius: 16px;
            text-decoration: none;
            color: var(--text-primary);
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
            border: 1px solid rgba(255, 255, 255, 0.05);
        }

        .dashboard-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(135deg, rgba(59, 130, 246, 0.1), transparent);
            opacity: 0;
            transition: opacity 0.3s ease;
        }

        .dashboard-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 30px -10px rgba(0, 0, 0, 0.5);
            border-color: rgba(59, 130, 246, 0.3);
        }

        .dashboard-card:hover::before {
            opacity: 1;
        }

        .card-icon {
            font-size: 2.5rem;
            margin-bottom: 1.5rem;
            background: linear-gradient(135deg, #3b82f6, #8b5cf6);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .card-content {
            position: relative;
            z-index: 1;
        }

        .card-title {
            font-size: 1.25rem;
            margin: 0 0 0.5rem 0;
            font-weight: 600;
        }

        .card-desc {
            color: var(--text-secondary);
            font-size: 0.9rem;
            margin: 0;
            line-height: 1.5;
        }
    </style>
</head>

<body>
    <div class="dashboard-layout">
        <%- include('partials/nav') %>

            <div class="dashboard-grid">
                <a href="/admin/edit/projects" class="dashboard-card">
                    <i class="fas fa-laptop-code card-icon"></i>
                    <div class="card-content">
                        <h3 class="card-title">Projects</h3>
                        <p class="card-desc">Manage your portfolio showcase items, tech stacks, and links.</p>
                    </div>
                </a>

                <a href="/admin/edit/services" class="dashboard-card">
                    <i class="fas fa-concierge-bell card-icon"></i>
                    <div class="card-content">
                        <h3 class="card-title">Services</h3>
                        <p class="card-desc">Update your service offerings and descriptions.</p>
                    </div>
                </a>

                <a href="/admin/edit/about" class="dashboard-card">
                    <i class="fas fa-user-astronaut card-icon"></i>
                    <div class="card-content">
                        <h3 class="card-title">About Me</h3>
                        <p class="card-desc">Update your bio, skills, and personal information.</p>
                    </div>
                </a>

                <a href="/admin/edit/blog" class="dashboard-card">
                    <i class="fas fa-newspaper card-icon"></i>
                    <div class="card-content">
                        <h3 class="card-title">Blog Posts</h3>
                        <p class="card-desc">Write and manage your latest articles and thoughts.</p>
                    </div>
                </a>
            </div>
    </div>
</body>

</html>