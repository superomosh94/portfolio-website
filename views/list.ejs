<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Manage <%= section %>
    </title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        :root {
            --bg-dark: #0f172a;
            --bg-card: #1e293b;
            --text-primary: #f8fafc;
            --text-secondary: #94a3b8;
            --accent-primary: #3b82f6;
            --danger: #ef4444;
            --success: #22c55e;
            --border: rgba(255, 255, 255, 0.1);
        }

        body {
            font-family: 'Inter', system-ui, -apple-system, sans-serif;
            background-color: var(--bg-dark);
            color: var(--text-primary);
            margin: 0;
            min-height: 100vh;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 2rem 2rem 2rem;
        }

        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 2rem;
            padding-bottom: 1rem;
            border-bottom: 1px solid var(--border);
        }

        .header h2 {
            font-size: 1.8rem;
            margin: 0;
            background: linear-gradient(135deg, #3b82f6, #8b5cf6);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .btn {
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            padding: 0.6rem 1.2rem;
            border-radius: 8px;
            text-decoration: none;
            font-size: 0.9rem;
            font-weight: 500;
            transition: all 0.2s;
            border: none;
            cursor: pointer;
        }

        .btn-primary {
            background: var(--accent-primary);
            color: white;
        }

        .btn-primary:hover {
            background: #2563eb;
            transform: translateY(-1px);
        }

        .btn-edit {
            background: rgba(59, 130, 246, 0.1);
            color: #60a5fa;
        }

        .btn-edit:hover {
            background: rgba(59, 130, 246, 0.2);
        }

        .btn-delete {
            background: rgba(239, 68, 68, 0.1);
            color: #fca5a5;
        }

        .btn-delete:hover {
            background: rgba(239, 68, 68, 0.2);
        }

        .item-list {
            list-style: none;
            padding: 0;
            margin: 0;
            display: flex;
            flex-direction: column;
            gap: 1rem;
        }

        .item-row {
            background: var(--bg-card);
            padding: 1.5rem;
            border-radius: 12px;
            border: 1px solid var(--border);
            display: flex;
            justify-content: space-between;
            align-items: center;
            transition: border-color 0.2s;
        }

        .item-row:hover {
            border-color: rgba(59, 130, 246, 0.3);
        }

        .item-info h4 {
            margin: 0 0 0.5rem 0;
            font-size: 1.1rem;
            color: var(--text-primary);
        }

        .item-desc {
            color: var(--text-secondary);
            font-size: 0.9rem;
            margin: 0;
            display: -webkit-box;
            -webkit-line-clamp: 2;
            -webkit-box-orient: vertical;
            overflow: hidden;
        }

        .actions {
            display: flex;
            gap: 0.8rem;
            margin-left: 2rem;
        }

        .alert-success {
            background: rgba(34, 197, 94, 0.1);
            color: #4ade80;
            padding: 1rem;
            border-radius: 8px;
            margin-bottom: 1.5rem;
            border: 1px solid rgba(34, 197, 94, 0.2);
        }
    </style>
</head>

<body>
    <%- include('partials/nav') %>
        <div class="container">

            <div class="header">
                <h2>Manage <%= section.charAt(0).toUpperCase() + section.slice(1) %>
                </h2>
                <a href="/admin/edit/<%= section %>/new" class="btn btn-primary"><i class="fas fa-plus"></i> Add New</a>
            </div>

            <!-- Flash messages can be added here if passed -->

            <ul class="item-list">
                <% items.forEach(item=> { %>
                    <li class="item-row">
                        <div class="item-info">
                            <h4>
                                <%= item.title || item.bio?.substring(0, 50) || ('Item #' + item.id) %>
                            </h4>
                            <p class="item-desc">
                                <%= item.description?.substring(0, 100) || item.excerpt?.substring(0, 100)
                                    || 'No description' %>...
                            </p>
                        </div>
                        <div class="actions">
                            <a href="/admin/edit/<%= section %>/<%= item.id %>" class="btn btn-edit"> <i
                                    class="fas fa-edit"></i> Edit</a>
                            <a href="/admin/delete/<%= section %>/<%= item.id %>" class="btn btn-delete"
                                onclick="return confirm('Are you sure you want to delete this item?')"><i
                                    class="fas fa-trash-alt"></i> Delete</a>
                        </div>
                    </li>
                    <% }) %>
            </ul>
        </div>
</body>

</html>